name: Cache Test

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache
          key: cache-5613487

      - name: prime-cache-5613487
        run: echo "Cache hit? ${{ steps.cache.outputs.cache-hit }}"
